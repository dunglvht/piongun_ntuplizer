To run this code, first scram the project using
```
scram b
```

then run
```
cmssw-el9
```
Once, you are inside singularity you can run it with:
```
source /cvmfs/cms.cern.ch/cmsset_default.sh
vocms
cmsenv
cmsRun test.y
```